include(source_files.cmake)

list(APPEND SOURCE_FILES
  ${ARRAY_STRING_SOURCE_FILES}
  ${HASHMAP_SOURCE_FILES}
  ${MATRIX_SOURCE_FILES}
  ${SLIDING_WINDOW_SOURCE_FILES}
  ${TWO_POINTERS_SOURCE_FILES}
  ${INTERVALS_SOURCE_FILES}
)

add_library(InterviewQuestions STATIC ${SOURCE_FILES})
